import 'package:flutter/material.dart';

class AppColors {
  // Midnight Safari Palette
  static const Color primary = Color(0xFF2979FF);   // Electric Blue
  static const Color accent = Color(0xFFFFD600);    // Neon Yellow/Gold
  static const Color playGreen = Color(0xFF00E676); // Spring Green
  static const Color error = Color(0xFFFF5252);     // Bright Red

  // Deep Gradient (Navy Blue to Pure Black)
  static const Color bgTop = Color(0xFF0D1B2A);     // Gunmetal Navy
  static const Color bgBottom = Color(0xFF000000);  // Pure Black

  static const Color fieldBg = Color(0xFF1B263B);   // Dark Slate for Inputs
  static const Color textMain = Colors.white;       // Pure White Text
}