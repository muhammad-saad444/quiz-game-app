name: realtime_answer_detector
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0
  provider: ^6.1.5+1
  animate_do: ^4.2.0
  http: ^1.6.0
  google_fonts: ^8.0.2
  fl_chart: ^1.2.0
  permission_handler: ^12.0.1

  # 👇 UPDATED PACKAGE NAME
  vosk_flutter_2: ^1.0.5
  record: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    # 👇 Registered offline voice model tracking folder
    - assets/models/

dependency_overrides:
  permission_handler: ^12.0.1